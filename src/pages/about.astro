---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import LinkButton from "@/components/LinkButton.astro";
import { SITE } from "@/config";
import IconSparkles from "@/assets/icons/IconSparkles.svg";

const { title = "Coming Soon" } = Astro.props;
---

<Layout title={`${title} | ${SITE.title}`}>
  <Header />

  <main
    id="main-content"
    class="app-layout flex flex-1 items-center justify-center min-h-[60vh]"
  >
    <div class="flex flex-col items-center justify-center text-center px-4">
      <div class="w-16 h-16 rounded-3xl bg-accent/10 flex items-center justify-center mb-8 animate-pulse">
        <IconSparkles class="w-8 h-8 text-accent" />
      </div>
      
      <h1 class="text-4xl sm:text-5xl font-bold text-main mb-4 tracking-tight">
        {title}
      </h1>
      
      <p class="text-dim/80 text-lg max-w-md mb-10 leading-relaxed">
        I'm currently building something special here. This page is being refined and will be available soon.
      </p>

      <div class="flex flex-col sm:flex-row gap-4">
        <LinkButton
          href="/"
          class="px-8 py-3 rounded-2xl bg-accent text-white font-bold hover:scale-105 transition-transform"
        >
          Return Home
        </LinkButton>
        <LinkButton
          href="/posts"
          class="px-8 py-3 rounded-2xl bg-border/20 text-main font-bold hover:bg-border/40 transition-all border border-border/50"
        >
          Read Blog
        </LinkButton>
      </div>

      <div class="mt-16 pt-8 border-t border-border/50 w-full max-w-sm">
        <p class="text-[10px] font-black uppercase tracking-[0.2em] text-dim/40 italic">
          Curating the technical pulse...
        </p>
      </div>
    </div>
  </main>

  <Footer />
</Layout>
